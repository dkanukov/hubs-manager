import {NuxtPage} from '#components';

export default defineComponent({
    setup() {
        return () => (
            <div>
                1
                <NuxtPage/>
            </div>
        )
    }
})
